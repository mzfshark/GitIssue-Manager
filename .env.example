# Example environment variables for GitIssue-Manager

# Default assignee used by scripts when GitHub viewer is not available
GITISSUE_DEFAULT_ASSIGNEE=mzfshark

# Local PLAN root (optional) - used as fallback when extracting PLAN.md locally
PLAN_ROOT=/path/to/your/local/repos

# Cleanup config path (can be a glob). Default used by scripts when not passed via --config
GITISSUE_CLEANUP_CONFIG=sync-helper/configs/*.json

# Logging
LOG_DIR=./logs

# Rate limiting / parallelism
MAX_PARALLEL=5

# Comment on close behavior
COMMENT_ON_CLOSE_ENABLED=true
CLOSE_MESSAGE=This issue is marked as not-implemented.

# GitHub token is typically provided to gh via gh auth login; if you prefer env-based
# authentication, set GITHUB_TOKEN (note: gh CLI may ignore this when already authenticated)
GITHUB_TOKEN=
